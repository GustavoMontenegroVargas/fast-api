[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fast-api"
version = "0.1.0"
description = "This repo contains the working activities of Fast API"
authors = [
    { name = "Gustavo Montenegro", email = "c.montenegro2402@gmail.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

dependencies = [
    "pandas>=2.3.3",
]

[project.optional-dependencies]

dev = [
    "pytest",
    "pytest-cov",
    "black",
    "isort",
    "mypy",
    "flake8",
    "ipykernel",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build]
packages = [
    "src",
    "consts.py"
]
